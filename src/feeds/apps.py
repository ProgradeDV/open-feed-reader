"""App config for Feeds."""
from django.apps import AppConfig


class FeedsConfig(AppConfig):
    """App config for Feeds."""

    name = "feeds"
    default_auto_field = "django.db.models.AutoField"
